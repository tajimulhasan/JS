var colorBank = ['lal', 'nil', 'holud', 'sobuj'];

    // something add in array
// colorBank.push('shadaa');
colorBank.pop();
console.log(colorBank);
var friendAge = [22, 12, 18, 17, 19, 24];

// friendAge.push(23);


// remove from array
friendAge.pop();
friendAge.shift();
console.log(friendAge);

// somthing add first in array doing with .unshift(something)

// somthing remove first in array doing with .shift()